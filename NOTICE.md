# Placeholder Asset Notice

All content in this MVP is placeholder, generated by tooling, meant to be replaced:

- **Images**: `Scripts/generate_placeholder_images.swift` renders a colored background + system emoji per word using AppKit — no external images, no licensing concerns. Replace any `<word_id>.png` inside `BabyFirstWords/Assets.xcassets/<word_id>.imageset/` with a real photo at any size (SwiftUI scales it to fit).
- **Audio**: `Scripts/generate_placeholder_audio.sh` uses the macOS `say` command (on-device TTS, voices "Samantha" for English and "Monica" for Spanish) to generate every word's pronunciation. Replace any file in `BabyFirstWords/Resources/Audio/en/` or `BabyFirstWords/Resources/Audio/es/` with a real recording — same filename, `.m4a` format.
- **App icon**: `BabyFirstWords/Assets.xcassets/AppIcon.appiconset/icon.png` is a placeholder emoji icon. Replace with a real 1024x1024 icon before any public release.

See `README.md` for the exact schema to add new words/lessons.
